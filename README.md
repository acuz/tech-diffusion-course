# Technological Diffusion — course slides

Live (RevealJS): served via GitHub Pages. Access is password-gated (client-side).

This repo holds the **rendered** deck only. Source lives in the
`claude-research-workflow` repo under `Quarto/`. To update:
`quarto render Quarto/tech_diffusion.qmd`, then copy `tech_diffusion.html` (as
`index.html`), `tech_diffusion_files/`, `images/`, `quiz.html`, `leaderboard.html` here and push.
